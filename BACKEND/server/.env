PORT=5001
SESSION_SECRET=change_this_to_a_long_random_secret

# Comma-separated list of allowed frontend origins
# Example: http://127.0.0.1:5500,http://localhost:5500
FRONTEND_ORIGINS=http://127.0.0.1:4012,http://localhost:4012

DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=password
DB_NAME=kapitbisig_db

# SMTP email (leave blank to disable email features)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@kapitbisig.ph

# Admin email — receives new campaign submission notifications
ADMIN_EMAIL=
